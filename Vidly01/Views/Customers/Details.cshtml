@model Vidly01.Models.Customer
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>
<ul>
    <li>Membership Tyoe: @Model.MembeshipType.Name</li>
    @if (Model.DOB.HasValue)
    {
        <li>DOB: @Model.DOB</li>
    }
</ul>

